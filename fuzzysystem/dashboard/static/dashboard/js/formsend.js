document.addEventListener("DOMContentLoaded", (ev) => {
  const formEl = document.getElementById("formElement");
  const btnSumbitEl = document.getElementById("btnSubmit");
  const inputFileEl = document.getElementById("file-upload");

  //   inputFileEl.addEventListener("change", () => {
  //     btnSumbitEl.disabled = false;
  //   });

  //   formEl.addEventListener("submit", () => {
  //     btnSumbitEl.disabled = true;
  //     inputFileEl.disabled = true;
  //   });
});
