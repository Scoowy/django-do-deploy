version: "3"

services:
  djangoapp:
    ports:
      - "80:8000" # we'll use port 5000 for django
    environment:
      - DJANGO_ENV=production
      - DJANGO_DEBUG=False
      - DJANGO_SYMANTO_API_KEY=6dd8dcb6acmsh0ae0691ab41c058p119ad5jsnd876ea394d69
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
      - POSTGRES_PORT=5432

  db:
    ports:
      - "5435:5432"  # pointing port 5435 from our environment to postgre default port 5432 in docker
    environment:
      POSTGRES_PASSWORD: 'postgres'
      POSTGRES_USER: 'postgres'
      POSTGRES_DB: 'djangoapp'
      POSTGRES_PORT: 5432